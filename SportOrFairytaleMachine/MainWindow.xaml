<Window x:Class="SportOrFairytaleMachine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SportOrFairytaleMachine" 
        xmlns:viewmodel="clr-namespace:SportOrFairytaleMachine.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1000">
    <Window.DataContext>
        <viewmodel:MainViewModel/>
    </Window.DataContext>
    <StackPanel>
        <ContentControl Width="1000" Height="700" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Content="{Binding CurrentView}"></ContentControl>
    </StackPanel>
</Window>
